# Required Environment Variables

# Session Secret - Generate a random string for production
# You can generate one with: openssl rand -base64 32
SESSION_SECRET=your-session-secret-here-change-in-production

# Database Connection String (PostgreSQL)
# Format: postgresql://user:password@host:port/database
# For local development with Neon, you can get this from your Neon dashboard
DATABASE_URL=postgresql://user:password@host:port/database

# Optional: Object Storage (for file uploads)
# If using Replit Object Storage, set this to your bucket ID
# DEFAULT_OBJECT_STORAGE_BUCKET_ID=your-bucket-id

# Optional: Authentication Bypass (Development Only)
# Set to "true" to bypass authentication in development mode
# ⚠️ WARNING: This will NOT work in production (automatically disabled)
# Set to "false" or leave unset to require authentication
BYPASS_AUTH=false

# Optional: Port (defaults to 5000)
PORT=5000

# Optional: Node Environment
# Set to "production" for production deployment
NODE_ENV=development
